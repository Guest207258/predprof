services:
  nginx:
    image: nginx:latest
    ports:
      - 810:80
    volumes:
      - ./scp:/usr/www
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - php
  php:
    build: ./php
    volumes:
      - ./scp:/usr/www
